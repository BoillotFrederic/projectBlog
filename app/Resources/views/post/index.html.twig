{% extends 'base.html.twig' %}

{% block body %}
    <h1>Posts list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Img</th>
                <th>Text</th>
                <th>Modified</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td><a href="{{ path('post_index', { 'id': post.id }) }}">{{ post.id }}</a></td>
                <td>{{ post.img }}</td>
                <td>{{ post.text }}</td>
                <td>{% if post.modified %}{{ post.modified|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if post.created %}{{ post.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('post_edit', { 'id': post.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('post_new') }}">Creer un nouvel article</a>
        </li>
    </ul>
{% endblock %}
